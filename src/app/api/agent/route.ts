// @deprecated: This route is superseded by /api/chat which uses the unified knowledge workflow.
// Kept for backwards compatibility. Remove after all clients migrate.
export { POST } from "@/app/api/chat/route";
